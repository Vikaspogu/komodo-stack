---
version: "3"
services:
  syncthing:
    image: ghcr.io/syncthing/syncthing:v2@sha256:6121dd07b5074e6f4a7c5da65cecacb6218bb09217175d44bfda5ab622f01196
    container_name: syncthing
    hostname: syncthing
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.syncthing.tls=true"
      - "traefik.http.routers.syncthing.rule=Host(`syncthing.synlo.duckdns.org`)"
      - "traefik.http.routers.syncthing.entrypoints=websecure"
      - "traefik.http.services.syncthing.loadbalancer.server.port=8384"
      - "homepage.group=traefik"
      - "homepage.name=syncthing"
      - "homepage.href=https://syncthing.synlo.duckdns.org"
      - "homepage.icon=https://cdn.jsdelivr.net/gh/selfhst/icons/png/syncthing.png"
    environment:
      - PUID=0
      - PGID=0
    volumes:
      - /srv/dev-disk-by-uuid-9d2e4cb9-8825-4168-8153-e020ee524474/storage0/syncthing:/var/syncthing
      - /srv/dev-disk-by-uuid-9d2e4cb9-8825-4168-8153-e020ee524474/storage0/pogu-nas/immich-upload:/immich-upload
    ports:
      - 8384:8384 # Web UI
      - 22000:22000/tcp # TCP file transfers
      - 22000:22000/udp # QUIC file transfers
      - 21027:21027/udp # Receive local discovery broadcasts
    restart: unless-stopped
    healthcheck:
      test: curl -fkLsS -m 2 127.0.0.1:8384/rest/noauth/health | grep -o --color=never OK || exit 1
      interval: 1m
      timeout: 10s
      retries: 3
    networks:
      - traefik_traefik

networks:
  traefik_traefik:
    external: true
